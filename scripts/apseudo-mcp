#!/usr/bin/env bash
# Compatibility entrypoint for callers that still invoke scripts/apseudo-mcp.
# The runnable MCP wrapper lives under mcp/ so active configs target that tree.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/mcp/apseudo-mcp" "$@"
