**Last updated:** 2026-07-20

# Sessions — 2026-07

## 2026-07-08

Adopted agent-handoff-v3 and six project-standards standards per TODO.md, via `docs/superpowers/plans/2026-07-08-adopt-standards.md`.

## 2026-07-09

Merged standards-adoption branch to `main` (`4ac4d37`), hand-reconciling a `TODO.md` conflict against concurrently-added user tasks. Recorded 4 pre-existing bugs found during adoption as `docs/handoff/bugs/001`-`004` (mcp.py stale paths, review.py stale paths, pre-migration transcript lint errors, lsp.py unhandled read). Routed durable facts out of an over-budget `state.md` into `conventions.md` C-004/C-005.

Moved agent-pseudocode lifecycle hook configs under `hooks/`, restored agent-handoff-v3 SessionStart hooks to canonical `.claude/hooks/` and `.codex/hooks/` paths, and moved MCP wrapper/config examples under `mcp/`.

Accepted ADR-0003 for durable Markdown frontmatter corpus and field conventions, then migrated the covered durable Markdown corpus and widened `.project-standards.yml` frontmatter validation to the ADR-approved scope.

Fixed bug 003 by excluding `docs/reference/pre-migration` from APSEUDO Markdown-fence linting so the verbatim historical transcript stays archived without blocking changed-file pseudocode checks.

## 2026-07-09 — Agent Handoff v1 migration

Migrated the dual Claude/Codex profile and canonical status/task paths to Agent Handoff v1 while preserving the user-owned task section.

## 2026-07-20 — project-standards V5.2.0 migration assessed (not applied)

Re-ran the V4→V5 migration assessment against the latest release, project-standards 5.2.0. Confirmed 5.2.0 fixes the prior upstream blockers (#8 subset-adopt crash, #9 `standards_version` gate, #10 pristine `.editorconfig` digest) and ships #11 bounded takeover for shared/instruction files. The read-only preview (`init --catalog 5 --migrate`) now reconciles 31 targets but stays `applicable: false` on three deliberately-customized files — genuine repository-intent conflicts, not tool bugs:

- `.editorconfig` `[*] indent_style` (repo `space` vs package-owned `tab`) — correct tool behavior: the repo's own `.prettierrc.json` is byte-identical to the package and emits tabs (`useTabs: true`), so the current `space` value contradicts it. Bounded takeover preserves the deliberate `[*.{apseudo,agentpseudo,pseudocode,py}] space/4` glob.
- `.github/workflows/format.yml` (`prettier: false` + `@v4` pin) — covered by open upstream #12 (markdown-tooling `legacy-format-caller` has no consumer-owned relinquishment).
- `.github/workflows/cli-docs-check.yml` (SHA-pinned action-hardening) — filed upstream as #13 (cli-documentation `legacy-workflow`: whole-file, single digest, no relinquishment pointer despite the package documenting the file as consumer-owned); cross-linked from #12.

Migration not applied; repo stays on V4 `.project-standards.yml` (fully functional; V5 keeps a read-only V4 fallback, removed in V6). Working tree untouched — preview writes nothing. Applying later needs an intent decision: align `.editorconfig` to `tab`, preserve the two hardened workflows (blocked on #12/#13 relinquishment, or accept restore + re-harden), and sequence the deferred lint/Prettier/spec CI gates the plan would create.

## 2026-07-22 — project-standards V4→V5 migration applied on release 5.4.0

Retired `.project-standards.yml` for the `.standards/` control plane. Upstream #12/#13 are closed, so 5.4.0 unblocked the workflow conflicts. Six packages, all `latest`: adr 1.2, agent-handoff 1.3, cli-documentation 1.3, markdown-frontmatter 1.4, markdown-tooling 1.6, project-spec 1.3.

Intent decisions resolved:

- `.editorconfig` `[*] indent_style` aligned to package-owned `tab`; the deliberate apseudo/py `space/4` glob survives via bounded takeover.
- `cli-docs-check.yml` declared `cli_documentation.workflow_ownership: consumer-owned`, keeping the SHA-pinned action hardening outside reconciliation.
- `format.yml` moved to managed ownership at `@v5` with `format = false` + `ci.format_caller = false`, replacing the old `@v4` + `prettier: false` caller.
- `lint-markdown.yml` and `validate-specs.yml` created with automatic triggers, per user decision to enable both gates now.

Two gates are red until their backlogs clear: markdownlint (272 pre-existing errors, unchanged by migration) and `spec validate` (no `docs/specs/` corpus yet). `docs/usage.md` landed as a `toolname` scaffold and still needs authoring for `apseudo`.

Filed upstream #16 (migration drops the legacy `prettier: false` caller input), #17 (project-spec migrates `ci=true` and errors on an empty corpus), #18 (managed instruction blocks count against agent-handoff size caps).

Checking CI after the push surfaced bug 005: `apseudo-lint.yml` validated the long-deleted `.codex/hooks.json`, so `bash -e` aborted the step and silently skipped ruff, basedpyright, and pip-audit on every commit since 2026-07-09. Fixed the stale path and ran the masked steps locally. Also fixed `_regen_index.py`, whose compact `|---|` separator and unescaped `*`/`_` regressed markdownlint by 10 errors, and added `tests/test_bug_index.py` to cover it.
